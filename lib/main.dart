import 'package:flutter/material.dart';

void main() {
	runApp(
		MaterialApp(
			title: "Purchase for Me",
			home: Scaffold(
				appBar: AppBar(
					title: Text("Purchase for Me"),
					centerTitle: true,
				),
				body: Container(
					child: Text("Hallo"),
				)
			),
		)
	);
}